/// <reference types="./types" />
import { Pair, InputStream, Bitmap } from "types";
export default class converters {
    constructor();
    static pairArrayAsync(...data: number[]): Promise<Array<Pair<number, number>>>;
    static pairArray(...data: number[]): Array<Pair<number, number>>;
    static pairArraysAsync(...data: any[]): Promise<Array<Array<Pair<number, number>>>>;
    static pairArrays(...data: any[]): Array<Array<Pair<number, number>>>;
    static sdToStreamAsync(filePath: string): Promise<InputStream>;
    static sdToStream(filePath: string): InputStream;
    static assetsToStreamAsync(filePath: string): Promise<InputStream>;
    static assetsToStream(filePath: string): InputStream;
    static streamToBitmapAsync(inputStream: InputStream): Promise<Bitmap>;
    static streamToBitmap(inputStream: InputStream): Bitmap;
    static bitmapToStreamAsync(bitmap: Bitmap): Promise<InputStream>;
    static bitmapToStream(bitmap: Bitmap): InputStream;
}
