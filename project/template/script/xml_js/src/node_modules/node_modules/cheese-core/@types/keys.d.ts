export default class keys {
    constructor();
    static homeAsync(): Promise<boolean>;
    static home(): boolean;
    static backAsync(): Promise<boolean>;
    static back(): boolean;
    static quickSettingsAsync(): Promise<boolean>;
    static quickSettings(): boolean;
    static powerDialogAsync(): Promise<boolean>;
    static powerDialog(): boolean;
    static pullNotificationBarAsync(): Promise<boolean>;
    static pullNotificationBar(): boolean;
    static recentsAsync(): Promise<boolean>;
    static recents(): boolean;
    static lockScreenAsync(): Promise<boolean>;
    static lockScreen(): boolean;
    static screenShotAsync(): Promise<boolean>;
    static screenShot(): boolean;
    static splitScreenAsync(): Promise<boolean>;
    static splitScreen(): boolean;
}
