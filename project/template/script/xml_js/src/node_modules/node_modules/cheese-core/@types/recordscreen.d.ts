/// <reference types="./types" />
import { Bitmap } from "types";
export default class recordscreen {
    constructor();
    static requestPermissionAsync(timeout: number): Promise<boolean>;
    static requestPermission(timeout: number): boolean;
    static checkPermissionAsync(tag: number): Promise<boolean>;
    static checkPermission(tag: number): boolean;
    static captureScreenAsync(timeout: number, x: number, y: number, ex: number, ey: number): Promise<Bitmap>;
    static captureScreen(timeout: number, x: number, y: number, ex: number, ey: number): Bitmap;
}
