/// <reference path="../types.d.ts" />
import { Bitmap, IntArray } from "types";
export default class Colors {
    constructor();
    static getRgbAsync(bitmap: Bitmap, x: number, y: number): Promise<IntArray>;
    static getRgb(bitmap: Bitmap, x: number, y: number): IntArray;
}
