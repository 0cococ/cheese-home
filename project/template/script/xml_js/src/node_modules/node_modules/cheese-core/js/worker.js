export default class worker {
    constructor() {
    }
    static createAsync(path, paths) {
        return WorkerApi.createAsync(path, paths);
    }
    static create(path, paths) {
        return WorkerApi.create(path, paths);
    }
}
