export default class time {
    constructor();
    static timeFormatAsync(timestamp: number, pn: string): Promise<string>;
    static timeFormat(timestamp: number, pn: string): string;
    static getTimeAsync(): Promise<number>;
    static getTime(): number;
}
