export default class assets {
    constructor();
    static readAsync(path: string): Promise<string>;
    static read(path: string): string;
    static copyAsync(path: string, destPath: string): Promise<boolean>;
    static copy(path: string, destPath: string): boolean;
    static isFolderAsync(path: string): Promise<boolean>;
    static isFolder(path: string): boolean;
    static isFileAsync(path: string): Promise<boolean>;
    static isFile(path: string): boolean;
}
