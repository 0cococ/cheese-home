/// <reference path="../types.d.ts" />
import { View, Callback } from "types";
export default class Xml {
    constructor();
    static parseXmlAsync(path: string): Promise<View>;
    static parseXml(path: string): View;
    static startActivityAsync(activityId: string, iActivity: Callback): Promise<void>;
    static startActivity(activityId: string, iActivity: Callback): void;
    static getIDAsync(id: string): Promise<number>;
    static getID(id: string): number;
}
