/// <reference types="./types" />
import { WorkerUtils } from "types";
export default class worker {
    constructor();
    static createAsync(path: string, paths: string): Promise<WorkerUtils>;
    static create(path: string, paths: string): WorkerUtils;
}
