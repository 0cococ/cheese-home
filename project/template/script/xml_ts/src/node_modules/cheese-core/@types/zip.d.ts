export default class zip {
    constructor();
    static compressAsync(srcFilePath: string, destFilePath: string, password?: string): Promise<boolean>;
    static compress(srcFilePath: string, destFilePath: string, password?: string): boolean;
    static decompressAsync(zipFilePath: string, destFilePath: string, password?: string): Promise<boolean>;
    static decompress(zipFilePath: string, destFilePath: string, password?: string): boolean;
}
