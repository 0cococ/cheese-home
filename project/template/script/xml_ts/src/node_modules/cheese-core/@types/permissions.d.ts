export default class permissions {
    constructor();
    static readonly ACCESSIBILITY: number;
    static readonly FLOAT: number;
    static readonly RECORDSCREEN: number;
    static requestPermissionAsync(tag: number, timeout: number): Promise<boolean>;
    static requestPermission(tag: number, timeout: number): boolean;
    static checkPermissionAsync(tag: number): Promise<boolean>;
    static checkPermission(tag: number): boolean;
}
