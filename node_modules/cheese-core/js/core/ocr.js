export default class ocr {
    constructor() {
    }
    static CHEESE = OcrApi._CHEESE;
    static LATIN = OcrApi._LATIN;
    static mlkitOcrAsync(bitmap, recognizer, callback) {
        return OcrApi.mlkitOcrAsync(bitmap, recognizer, callback);
    }
    static mlkitOcr(bitmap, recognizer, callback) {
        return OcrApi.mlkitOcr(bitmap, recognizer, callback);
    }
}
