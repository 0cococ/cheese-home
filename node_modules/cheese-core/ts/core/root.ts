declare var RootApi: any
export default class root {
    constructor() {
    }

    public static requestPermissionAsync(timeout:number): Promise<boolean> {
        return RootApi.requestPermissionAsync(timeout);
    }

    public static requestPermission(timeout:number): boolean {
        return RootApi.requestPermission(timeout);
    }


    public static checkPermissionAsync(): Promise<boolean> {
        return RootApi.checkPermissionAsync();
    }

    public static checkPermission(): boolean {
        return RootApi.checkPermission();
    }

    public static execAsync(command: string): Promise<string> {
        return RootApi.exec(command);
    }

    public static exec(command: string): string {
        return RootApi.exec(command);
    }

    public static parseEventAsync(str: string): Promise<string> {
        return RootApi.parseEventAsync(str);
    }

    public static parseEvent(str: string): string {
        return RootApi.parseEvent(str);
    }
}