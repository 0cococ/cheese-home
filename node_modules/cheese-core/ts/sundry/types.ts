declare module "types" {
    export type List<T> = [T];
    export type EasyWindow<T> = [T];
    export type InputStream = {};
    export type Pair<T, U> = [T, U];
    export type Callback = {};
    export type Bitmap = {};
    export type Class<T> = [T];
    export type WorkerUtils = {};
    export type Rect = [left: number, top: number, right: number, bottom: number];
    export type IntArray = { x: string, y: string };
    export type View = {};
    export type Map<T, U> = [T, U];
    export type MutableList<T> = [T]
    export type Yolov8Ncnn = {
        Obj: any;
    };
    export type File = {
        path: string;
    };
    export type IConsoleClient = {
        show(): void;
        log(log: string): void;
        clear(): void;
        hide(): void;
        setTouch(enabled: boolean): void;
    };
    export type IToolWindowClient = {
        show(): void;
        hide(): void;
        cancel(): void;
    };

}
