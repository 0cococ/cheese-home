import { Bitmap, Callback } from "types";
export default class ocr {
    constructor();
    static readonly CHEESE: number;
    static readonly LATIN: number;
    static mlkitOcrAsync(bitmap: Bitmap, recognizer: number, callback: Callback): Promise<void>;
    static mlkitOcr(bitmap: Bitmap, recognizer: number, callback: Callback): void;
}
