import { Pair } from "types";
export default class point {
    constructor();
    static swipeToPointAsync(sx: number, sy: number, ex: number, ey: number, dur: number): Promise<boolean>;
    static swipeToPoint(sx: number, sy: number, ex: number, ey: number, dur: number): boolean;
    static clickPointAsync(x: number, y: number): Promise<boolean>;
    static clickPoint(x: number, y: number): boolean;
    static longClickPointAsync(x: number, y: number): Promise<boolean>;
    static longClickPoint(x: number, y: number): boolean;
    static touchDownAsync(x: number, y: number): Promise<boolean>;
    static touchDown(x: number, y: number): boolean;
    static touchMoveAsync(x: number, y: number): Promise<boolean>;
    static touchMove(x: number, y: number): boolean;
    static touchUpAsync(): Promise<boolean>;
    static touchUp(): boolean;
    static gestureAsync(dur: number, array: Array<Pair<number, number>>): Promise<boolean>;
    static gesture(dur: number, array: Array<Pair<number, number>>): boolean;
    static superSwipeToPoint(sx: number, sy: number, ex: number, ey: number, dur: number, xShake: number, yShake: number): boolean;
}
