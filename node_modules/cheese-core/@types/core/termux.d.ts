import { Callback } from "types";
export default class termux {
    private that;
    constructor();
    connectTermux(callback: Callback): void;
    close(): void;
    state(): boolean;
    read(): string;
    send(c: string): void;
    whileRead(callback: Callback): void;
    stopWhileRead(): void;
    whileReadState(): boolean;
    install(pkg: string): void;
    uninstall(pkg: string): void;
}
