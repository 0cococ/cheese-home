import { WorkerUtils } from "types";
export default class worker {
    constructor();
    static createAsync(path: string): Promise<WorkerUtils>;
    static create(path: string): WorkerUtils;
    static eval(code: string): void;
}
