import { EasyWindow } from "types";
export default class floaty {
    constructor();
    static floatWithAsync(): Promise<EasyWindow<any>>;
    static floatWith(): EasyWindow<any>;
    static requestPermissionAsync(timeout: number): Promise<EasyWindow<any>>;
    static requestPermission(timeout: number): EasyWindow<any>;
    static checkPermissionAsync(): Promise<boolean>;
    static checkPermission(): boolean;
    static getFloaty(): any;
    static drawToScreenAsync(text: string, left: number, top: number, right: number, bottom: number): Promise<any>;
    static drawToScreen(text: string, textColor: number, borderedColor: number, rect: any): any;
    static drawToScreen(text: string, rect: any): any;
}
