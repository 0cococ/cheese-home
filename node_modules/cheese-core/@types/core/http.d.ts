export default class http {
    constructor();
    static getDownloadAsync(url: String, savePath: String): Promise<boolean>;
    static getDownload(url: String, savePath: String): boolean;
    static getAsync(url: String): Promise<string>;
    static get(url: String): string;
}
