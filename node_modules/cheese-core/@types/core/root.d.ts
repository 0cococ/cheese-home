export default class root {
    constructor();
    static requestPermissionAsync(timeout: number): Promise<boolean>;
    static requestPermission(timeout: number): boolean;
    static checkPermissionAsync(): Promise<boolean>;
    static checkPermission(): boolean;
    static execAsync(command: string): Promise<string>;
    static exec(command: string): string;
    static parseEventAsync(str: string): Promise<string>;
    static parseEvent(str: string): string;
}
