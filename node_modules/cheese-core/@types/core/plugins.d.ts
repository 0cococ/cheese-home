export default class plugins {
    constructor();
    static installAsync(path: string): Promise<boolean>;
    static install(path: string): boolean;
    static startAsync(): Promise<void>;
    static start(): void;
    static uninstallAsync(pkg: string): Promise<boolean>;
    static uninstall(pkg: string): boolean;
}
