import { Callback, Map } from "types";
export default class vue {
    constructor();
    static getValueAsync(): Promise<Map<string, string>>;
    static getValue(): Map<string, string>;
    static onClickAsync(async: boolean, obj: any, callback: Callback): Promise<void>;
    static onClick(async: boolean, obj: any, callback: Callback): void;
}
